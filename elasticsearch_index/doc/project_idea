可优化的方面：
1.将indexModel这个索引实体由文件读取得到，做成可配置的，无需修改代码即可在ES中生成特定字段。（可开发一个后台应用，用来管理索引，可以配置索引的存储状态、字段类型以及是否分词等配置）
2.将索引设立别名，方便切换索引，索引的版本管理以及别名管理最好也是在后台应用管理
3.将创建索引以及创建mapping 和添加索引分别作为独立模块

待开发：
1.完善数据导入es的整个流程
2.实现简单的搜索功能
